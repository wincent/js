{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@babel/cli": "*",
    "@babel/core": "*",
    "@babel/preset-env": "*",
    "flow-bin": "*",
    "prettier": "*"
  },
  "scripts": {
    "build": "make all",
    "checkFormat": "prettier --list-different packages/*/src/**/*.js packages/*/*.js",
    "format": "prettier --write packages/*/src/**/*.js packages/*/*.js",
    "prepublishOnly": "yarn checkFormat && yarn typecheck",
    "typecheck": "flow"
  }
}
